using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BeefyBoi : Enemy
{

    void OnCollisionEnter(Collision col)
    {
       /* if (col.gameObject.tag == "electric") { 
        this.TakeDamage(Random.Range(0, 100));*/
    }
}
//}
